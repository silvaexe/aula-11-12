function x (numeros) {
    const novoArray=[]
    const numero1 = numeros [0] / 2
    const numero2 = numeros [numeros.length-1] / 2
    novoArray.push(numero2)
    novoArray.push(numero1)
    return novoArray
    
}

